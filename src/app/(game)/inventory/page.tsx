// Lightweight wrapper page that renders the client Inventory UI component

"use client";

import { InventoryClient } from "@/components/game/InventoryClient";

export default function InventoryPage() {
  return <InventoryClient />;
}
